#include "bits/stdc++.h"

using namespace std;
using ll = long long;
using ii = pair<int, int>;
using vi = vector<int>;


int main(){
	ios::sync_with_stdio(0);
	cin.tie(0);


	int t;
	cin >> t;
	while(t--){
		int d;
		cin >> n;

		while(d > 1e5){
			cout << min
		}

		if (d < 1e4){
			cout << d+1 << "\n";
			for(int i = 0;i < d;i++){
				cout << 2 << " ";
			}
			cout << 3 << "\n";
		} else {
				
		}

	}
	return 0;
}
