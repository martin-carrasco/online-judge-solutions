#include "bits/stdc++.h"
using namespace std;
using ll = long long;
using ii = pair<int, int>;
using vi = vector<int>;

const int MAXN = 2000 + 5;

multiset<int> a;
int main(){
	ios::sync_with_stdio(0);
	cin.tie(0);
	cin >> t;
	while(t--){
		cin >> n;
		N = 2*n;	
		for(int i = 0;i < N;i++) {
			int tmp;
			a.insert(tmp);
		}
			
		auto it_big = a.end();
		--it_big;
		int big = *it_big;

		for(int i = 0)
	}
	return 0;
}
